mutation CreateSubcategory(
  $name: String!,
  $categoryId: String!
  ) {
  createSubcategory(createSubcategoryInput: {
    name: $name,
    categoryId: $categoryId
  }){
    _id,
    name,
    isDeleted
  }
}
